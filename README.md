# MCMS

Development repository for the MSPEE-118 governed periodic-table element engine.

Useful operator checks:

```powershell
python -m mcms.cli elements --full-span-promotion-approval-decision
python -m mcms.cli elements --full-span-promotion-approval-decision
python -m mcms.cli elements --symbol Tc --readiness-score
python scripts\verify_repo.py
```
